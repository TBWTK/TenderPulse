name: tenderpulse
version: 1.0.0
config-version: 2
profile: tenderpulse
model-paths: ["models"]
clean-targets: ["target", "dbt_packages"]

models:
  tenderpulse:
    staging:
      +materialized: view
    marts:
      +materialized: table
